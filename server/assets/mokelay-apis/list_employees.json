{
  "uuid": "list_employees",
  "alias": "员工列表接口",
  "method": "GET",
  "request": {
    "query": [
      "page",
      "pageSize"
    ]
  },
  "blocks": [
    {
      "uuid": "starter",
      "nextBlock": "list_employees_block"
    },
    {
      "uuid": "list_employees_block",
      "alias": "员工分页 Block",
      "functionName": "page",
      "inputs": {
        "datasource": "Mokelay",
        "table": "employees",
        "fields": [
          "id",
          "enterprise_uuid",
          "name",
          "email",
          "plan",
          "created_at",
          "updated_at"
        ],
        "page": {
          "template": "{{request.query.page}}"
        },
        "pageSize": {
          "template": "{{request.query.pageSize}}"
        },
        "orderBy": [
          {
            "fieldName": "updated_at",
            "direction": "DESC"
          },
          {
            "fieldName": "created_at",
            "direction": "DESC"
          }
        ]
      },
      "outputs": [
        "datas",
        "total",
        "totalPages",
        "page",
        "pageSize",
        "hasPreviousPage",
        "hasNextPage"
      ],
      "nextBlock": null
    }
  ],
  "responses": {
    "list_employees_block": {
      "employees": {
        "template": "{{blocks['list_employees_block'].outputs.datas}}"
      },
      "pagination": {
        "page": {
          "template": "{{blocks['list_employees_block'].outputs.page}}"
        },
        "pageSize": {
          "template": "{{blocks['list_employees_block'].outputs.pageSize}}"
        },
        "total": {
          "template": "{{blocks['list_employees_block'].outputs.total}}"
        },
        "totalPages": {
          "template": "{{blocks['list_employees_block'].outputs.totalPages}}"
        },
        "hasPreviousPage": {
          "template": "{{blocks['list_employees_block'].outputs.hasPreviousPage}}"
        },
        "hasNextPage": {
          "template": "{{blocks['list_employees_block'].outputs.hasNextPage}}"
        }
      }
    }
  }
}
