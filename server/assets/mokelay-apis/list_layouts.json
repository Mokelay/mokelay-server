{
  "uuid": "list_layouts",
  "alias": "布局列表接口",
  "method": "GET",
  "request": {
    "query": [
      "page",
      "pageSize"
    ]
  },
  "blocks": [
    {
      "uuid": "starter",
      "nextBlock": "list_layouts_block"
    },
    {
      "uuid": "list_layouts_block",
      "alias": "布局分页 Block",
      "functionName": "page",
      "inputs": {
        "datasource": "Mokelay",
        "table": "layouts",
        "fields": [
          "uuid",
          "name",
          "layout_json",
          "created_at",
          "updated_at"
        ],
        "page": {
          "template": "{{request.query.page}}"
        },
        "pageSize": {
          "template": "{{request.query.pageSize}}"
        },
        "orderBy": [
          {
            "fieldName": "updated_at",
            "direction": "DESC"
          }
        ]
      },
      "outputs": [
        "datas",
        "total",
        "totalPages",
        "page",
        "pageSize",
        "hasPreviousPage",
        "hasNextPage"
      ],
      "nextBlock": null
    }
  ],
  "responses": {
    "list_layouts_block": {
      "layouts": {
        "template": "{{blocks['list_layouts_block'].outputs.datas}}"
      },
      "pagination": {
        "page": {
          "template": "{{blocks['list_layouts_block'].outputs.page}}"
        },
        "pageSize": {
          "template": "{{blocks['list_layouts_block'].outputs.pageSize}}"
        },
        "total": {
          "template": "{{blocks['list_layouts_block'].outputs.total}}"
        },
        "totalPages": {
          "template": "{{blocks['list_layouts_block'].outputs.totalPages}}"
        },
        "hasNextPage": {
          "template": "{{blocks['list_layouts_block'].outputs.hasNextPage}}"
        },
        "hasPreviousPage": {
          "template": "{{blocks['list_layouts_block'].outputs.hasPreviousPage}}"
        }
      }
    }
  }
}
