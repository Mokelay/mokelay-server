{
  "uuid": "read_mokelay_layout_json",
  "alias": "读取内置布局 DSL 接口",
  "method": "GET",
  "request": {
    "query": [
      "uuid"
    ]
  },
  "blocks": [
    {
      "uuid": "starter",
      "nextBlock": "read_mokelay_layout_json_block"
    },
    {
      "uuid": "read_mokelay_layout_json_block",
      "alias": "读取内置布局 DSL Block",
      "functionName": "readMokelayLayoutJson",
      "inputs": {
        "uuid": {
          "template": "{{request.query.uuid}}"
        }
      },
      "outputs": [
        "layout"
      ],
      "nextBlock": null
    }
  ],
  "responses": {
    "read_mokelay_layout_json_block": {
      "layout": {
        "template": "{{blocks['read_mokelay_layout_json_block'].outputs.layout}}"
      }
    }
  }
}
